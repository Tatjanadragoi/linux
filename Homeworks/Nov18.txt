ls - листинг каталогов/файлов

pwd (print working dir) - показать тек/каталог

cd (change dir) - смена каталога

(без аргументов - вернуться в дом/каталог)

mkdir (make dir) - создать каталог

rm (remove) - удалить файл/каталог

mv (move) - переместить/переименовать

cp (copy) - копировать

cat - вывод содержимого файла в терминал

id - вывод информации о пользователе

uname - вывод информации о системе
==
cd (перейти в дом/каталог)
mkdir backups (создать каталог)

~ (дом/каталог)
. (тек/каталог)
.. (каталог выше)

root - название пользователя по умолчанию
(главного администратора компьютера)

ls -lah
	вывести листинг каталога, где
	-l (списком)
	-a (скрытые)
	-h (с единицами изм.)

mkdir -p dir1/dir2/dir3

(p - parent)

создание иерархии каталогов

rm -rf dir

	удалить каталог
	-r (рекурсивно)
	-f (для каталогов)

ПУТЬ В ИЕРАРХИИ КАТАЛОГОВ В LINUX
	
	(1) Относительный
		(напр., dir1/dir2/dir3
		
	(2) Абсолютный
		(напр., /root/dir1/dir2/dir3)

df (disk free) - статистика по дискам
	
	df -h (часто применение)

du (disk usage) - просмотр статистики по каталогам

	du -hs (частое применение)


top (статистика по выч. ресурсам)


uptime (время непрерывной работы)
